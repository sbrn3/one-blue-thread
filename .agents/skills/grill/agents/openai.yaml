interface:
  display_name: "Grill"
  short_description: "Resolve product decisions before planning"
  default_prompt: "Use $grill to interview me until this change is precise enough to plan."
policy:
  allow_implicit_invocation: false
