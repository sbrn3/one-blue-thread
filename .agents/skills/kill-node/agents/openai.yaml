interface:
  display_name: "Kill Node"
  short_description: "Safely stop scoped Node background processes"
  default_prompt: "Use $kill-node to identify and stop the Node processes for this project."
policy:
  allow_implicit_invocation: false
