interface:
  display_name: "Catch Me Up"
  short_description: "Orient from a deterministic fact sheet (scripts/catch-me-up.mjs); --fix also repairs stale docs, reclassifies feature-request bugs, re-triages the roadmap, batches real bugs"
  default_prompt: "Use $catch-me-up to summarize where One Blue Thread's work stands and recommend the safest next action."
policy:
  allow_implicit_invocation: false
