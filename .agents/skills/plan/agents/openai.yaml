interface:
  display_name: "Plan"
  short_description: "Create a complexity-graded implementation plan"
  default_prompt: "Use $plan to design this change before any implementation work."
policy:
  allow_implicit_invocation: false
